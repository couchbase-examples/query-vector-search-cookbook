# Copy to .env, fill in secrets, then run: set -a; source .env; set +a
OPENAI_API_KEY=
CB_CONN_STR=couchbase://localhost
CB_USER=Administrator
CB_PASS=password
CB_BUCKET_NAME=test
CB_SCOPE_NAME=langgraph
